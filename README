Sometimes I need to modify some packages, espacially R packages.
So, I create this repo for such codes.

0. rpart_m
   This package is to modify rpart, an R package to compute recursive partitioning and regression tree. 